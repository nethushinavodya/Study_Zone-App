import React from 'react';

// LockScreen design removed per request. Keep a minimal component so import sites remain valid.
const LockScreen: React.FC = () => {
  return null;
};

export default LockScreen;
